export interface ProfileInterface {
    userId: string,
    name?: string,
    username: string,
    email: string,
    phoneNumber?: number,
    bio?: string,
    profilePic?: string,
    isPrivate?: boolean
    // savedPosts?: string[],
}

